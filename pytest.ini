[pytest]
markers =
  needs_key(name): skip this test if the given env var is not set
  live: tests that call external services

    # new markers used by your suite
    integration: tests that require a database or external service
    performance: latency/throughput checks
    regression: regression/compatibility tests
    unit: isolated unit tests
    system: end-to-end or UI workflow tests